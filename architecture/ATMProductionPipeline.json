{
	"kind": "architecture",
	"name": "Standard production pipeline architecture",
	"template": [
		{ "kind": "datasource", "rules": "1" },
		{ "kind": "filter,transformer,augmentor,splitter,viz,stats", "rules": "0+" },
		{ "kind": "predictor", "rules": "1" }
	]
}
