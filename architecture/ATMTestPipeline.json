{
	"kind": "architecture",
	"name": "Standard testing pipeline architecture",
	"template": [
		{ "kind": "datasource", "rules": "1" },
		{ "kind": "filter,transformer,augmentor,splitter,viz,stats", "rules": "0+" },
		{ "kind": "tester", "rules": "1" }
	]
}
